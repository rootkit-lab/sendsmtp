Show SendSMTP queue and campaign status.

1. Run `./bin/sendsmtp-cli status` and `./bin/sendsmtp-cli stats`.
2. Summarize pending/sent/failed, active vs disabled SMTPs, campaign state, rate, and top errors.
