# Example campaign files for SendSMTP docs
#
# Copy subjects into data/assuntos.txt (strip comment lines starting with #).
# Copy an HTML file to data/msg.html.
# Put base URLs in data/links.txt without ?p= — the engine adds ?p=<email>.
#
# See https://rootkit-lab.github.io/sendsmtp/campaigns.html
